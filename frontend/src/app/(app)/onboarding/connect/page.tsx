'use client'
import Link from 'next/link'
import {useApp} from '@/lib/app-context'
export default function Connect(){const {session,user}=useApp();return <main className="nexo-page max-w-lg mx-auto"><h1 className="text-2xl font-semibold">Conexão do WhatsApp</h1>{session?.status==='connected'?<><p className="my-5">WhatsApp conectado. As conversas recebidas pelo servidor aparecem neste PWA.</p><Link className="nexo-primary" href="/messages">Abrir conversas</Link></>:<><p className="my-5">No computador, abra a página abaixo e entre com a mesma conta deste PWA: <strong>{user?.email}</strong>.</p><Link className="text-link break-all" href="/connect">frontend-production-d0af.up.railway.app/connect</Link><p className="my-5">Escaneie o QR usando WhatsApp → Dispositivos conectados → Conectar dispositivo. Esta tela acompanha a conexão automaticamente.</p></>}<Link className="text-link block mt-6" href="/diagnostics">Ver diagnóstico de conexão e voz</Link></main>}
