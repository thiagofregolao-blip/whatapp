# Frontend

Veja o [README principal](../README.md). Execute `npm ci`, `npm run dev` ou `npm run build`. `/assistant` é a tela principal; `/onboarding/connect` conecta o WhatsApp. Páginas legadas incompletas estão arquivadas fora das rotas ativas.
